# Placeholder so the `public/` directory is tracked by git.
# Next.js serves any file in this folder at the root path (e.g. /favicon.ico).
# The Docker web image copies `apps/web/public` into the standalone bundle.
